import PageHeader from '@/components/ui/page/Header';

const FieldCategoryHeader: React.FC = () => {
  return (
    <div>
      <PageHeader
        isShowBtn
        titleButton="Add new Field Category"
        title="Field Category"
        items={[]}
        onClick={() => {}}
      />
    </div>
  );
};

export default FieldCategoryHeader;
